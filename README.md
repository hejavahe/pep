1. Что в терминологии реляционной модели данных соответствует понятию «кортеж»?
Ответ: Строка таблицы

2. Какие результаты могут принимать логические выражения в SQL в связи с поддержкой NULL-значений?
Ответ: TRUE (истина), FALSE (ложь), UNKNOWN (неизвестно)

3. В чем заключается особенность использования ROWS при определении границ окна?
Ответ: Основывается на физическом положении строк относительно текущей строки

4. Какие условия обязательны для того, чтобы представление было автоматически изменяемым?
Ответ: Отсутствие агрегатных функций в списке выборки;
Список FROM содержит одну таблицу или изменяемое представление

5. Установите соответствие между категорией оконной функции и её основным назначением.
Ответ:
Агрегатные — Выполняют арифметические вычисления на наборе данных
Ранжирующие — Ранжируют значение для каждой строки в окне
Функции смещения — Позволяют обращаться к разным строкам окна

6. Чем отличаются монопольные блокировки (X-блокировки) от разделяемых?
Ответ: Они запрещают любой взаимный доступ к объекту данных

7. Каков общий синтаксис оператора для соединения таблиц в языке SQL?
Ответ: SELECT поля FROM таблица_1 JOIN таблица_2 ON условие

8. Какие параметры можно передать в функцию LAG для настройки выборки?
Ответ: Смещение в строках, Значение по умолчанию

9. Какие операторы допустимы в предложении WHERE для реализации условий отбора по диапазону или шаблону?
Ответ: BETWEEN для поиска внутри интервала, LIKE для сравнения со строковым шаблоном

10. Установите соответствие между элементами работы с процедурами и их назначением.
Ответ:
INOUT — Передача и изменение данных
IN — Передача значений в процедуру
OUT — Возврат значений после выполнения
CALL — Оператор вызова процедуры

11. Какая секция оператора MERGE выполняется, если условие ON обнаружило соответствие между строками источника и цели?
Ответ: WHEN MATCHED

12. Какие строковые функции предназначены для удаления пробелов в SQL?
Ответ: TRIM(), LTRIM()

13. К какому типу оконных функций относятся LAG и LEAD?
Ответ: Функции смещения

14. В каких сферах деятельности применяется хранение двоичных данных?
Ответ: Облачные хранилища для документов разных форматов, Видеохостинги для размещения видеофайлов

15. Установите соответствие между оператором DML и его назначением
Ответ:
UPDATE — Обновление данных в столбце
MERGE — Слияние нескольких операций модификации
INSERT — Добавление записей в таблицу
DELETE — Удаление записей из таблицы

16. Какое выражение CASE используется только для проверки равенства входного выражения набору значений?
Ответ: Простое выражение CASE

17. Какие этапы запроса должны завершиться перед расчетом оконных функций?
Ответ: Группировка данных GROUP BY, Фильтрация строк WHERE

18. Какой результат формирует операция CROSS JOIN?
Ответ: Каждая строка из первой таблицы соединяется с каждой строкой из второй таблицы

19. Какие утверждения верны для операции LEFT ANTI-JOIN?
Ответ:
Возвращает только те строки из левой таблицы, для которых нет соответствий в правой
Исключает строки, имеющие совпадения в правой таблице
Может быть реализована через LEFT JOIN с условием B.KEY IS NULL

20. В чем заключаются различия между UNION и UNION ALL в SQL?
Ответ:
UNION исключает дубликаты из результирующего набора
UNION ALL сохраняет все дубликаты строк
Обе операции требуют совместимости типов данных

21. Что возвращает оператор INNER JOIN при объединении двух таблиц?
Ответ: Только те строки, которые имеют совпадение в заданном критерии в обеих таблицах

22. Как можно реализовать вещественное деление в PostgreSQL, чтобы избежать отбрасывания дробной части?
Ответ: Сделать один из операндов вещественным с помощью CAST, Использовать запись числа с точкой (например, 5./3)

23. Установите соответствие между строковой функцией и её назначением
Ответ:
SUBSTRING() — Извлечение подстрок
TRIM() — Удаление пробелов
REPLACE() — Замена подстрок
CONCAT() — Объединение строк

24. Каким будет результат выполнения функции CEIL(69.69)?
Ответ: 70

25. Какие правила и характеристики применимы к использованию подзапросов?
Ответ:
Скалярный подзапрос возвращает единственное значение
Текст подзапроса должен быть заключен в скобки
Допускается ссылка на столбцы внешнего запроса

26. Какие характеристики описывают обычные представления (VIEW)?
Ответ: Содержимое выбирается из других таблиц, Формируются динамически

27. Установите соответствие между командами управления курсорами и их функциями.
Ответ:
MOVE — перемещает курсор по набору данных
FETCH — извлекает строки из курсора
OPEN — открывает курсор
CLOSE — закрывает курсор

28. Какое свойство является преимуществом TRUNCATE по сравнению с командой DELETE?
Ответ: TRUNCATE работает значительно быстрее, так как не сканирует каждую строку

29. Какие условия должны соблюдаться для успешного выполнения операции UNION?
Ответ: Одинаковое количество выходных столбцов в каждом из запросов, Совместимость выходных столбцов по типам данных

30. Какие команды используются для непосредственного управления явными курсорами?
Ответ: OPEN, FETCH, CLOSE

31. Сопоставьте свойства транзакций с их определениями согласно стандарту ACID:
Ответ:
Атомарность — транзакция выполняется либо целиком, либо не выполняется совсем
Согласованность — переход базы данных из одного целостного состояния в другое
Долговечность — сохранение результатов выполненной работы даже в случае сбоя системы
Изоляция — транзакции разных пользователей не должны мешать друг другу

32. Каким оператором можно принудительно прервать выполнение бесконечного цикла LOOP?
Ответ: EXIT

33. Какие типы ограничений относятся к декларативным ограничениям (Declarative Constraints) в схеме базы данных?
Ответ: UNIQUE и CHECK, PRIMARY KEY и FOREIGN KEY

34. Как заполняются данные из правой таблицы в LEFT JOIN, если для строки из левой таблицы не найдено совпадения?
Ответ: Значениями NULL

35. Какие предложения (фразы) входят в состав общего формата команды выборки данных SELECT?
Ответ: FROM для указания источника записей, WHERE для условия отбора записей, ORDER BY для сортировки результата

36. Какие три логических результата возможны при работе с выражениями в SQL из-за поддержки NULL-значений?
Ответ: TRUE, FALSE и UNKNOWN

37. Какие способы явного приведения типов данных к целому числу (INTEGER) поддерживаются в PostgreSQL?
Ответ: CAST('123' AS INTEGER), '123' :: INTEGER

38. Какой оператор используется для объединения операций вставки, обновления и удаления в одну команду в зависимости от условий соединения?
Ответ: MERGE

39. Из каких основных элементов состоит механизм работы с курсором?
Ответ: Результирующий набор, Позиция курсора

40. Какая специальная переменная в триггерах уровня строки содержит данные, которые должны быть записаны в базу при операции INSERT?
Ответ: NEW

41. В чем заключается основное отличие материализованного представления от обычного?
Ответ: Оно физически хранит результаты выполнения запроса

42. Какие типы параметров могут использоваться в хранимых процедурах?
Ответ: Входные параметры (IN), Выходные параметры (OUT), Входные-выходные параметры (INOUT)

43. Какие типы внешних соединений предусмотрены в общем синтаксисе оператора JOIN?
Ответ: LEFT, RIGHT, FULL

44. Сопоставьте объект базы данных с его основной функцией
Ответ:
Таблицы — Хранение данных в базе
Индексы — Ускорение поиска записей
Представления — Виртуальные таблицы на базе запросов
Триггеры — Автоматическое выполнение при событиях

45. Что является результатом операции проекции (PROJECT) в реляционной алгебре?
Ответ: Отношение, кортежи которого являются подмножествами кортежей операнда (выбор конкретных атрибутов)

46. Какой тип данных используется для хранения больших объемов двоичной информации?
Ответ: BLOB (bytea)

47. Сопоставьте названия этапов процесса обработки данных ETL
Ответ:
Transform — Преобразование данных
Load — Загрузка данных
Extract — Извлечение данных

48. Какое предложение необходимо использовать в SQL-команде UPDATE для изменения строки, на которой сейчас позиционирован курсор?
Ответ: WHERE CURRENT OF

49. Что позволяет ускорить доступ к двоичным данным и оптимизировать ресурсы?
Ответ: Использование соответствующих индексов, Настройка специальных параметров хранения

50. Как по умолчанию выполняется операция деления (/) в PostgreSQL, если оба операнда являются целыми числами?
Ответ: Как целочисленное деление

51. В чём заключаются основные отличия хранимых процедур от функций в PostgreSQL?
Ответ: Могут включать транзакции, Вызываются отдельно командой CALL

52. Для выполнения какой задачи предназначен объект курсор в PostgreSQL?
Ответ: Для обеспечения построчного доступа к результирующему набору строк SQL-запроса

53. Какие функции позволяют получить текущие дату и время в системе?
Ответ: NOW(), CURRENT_TIMESTAMP

54. Какая функция возвращает текущую дату и время вместе с локальным часовым поясом?
Ответ: CURRENT_TIMESTAMP

55. Какая форма записи используется в SQL, когда столбцы соединения в обеих таблицах имеют идентичные имена?
Ответ: USING (список столбцов)

56. Для чего используется предложение PARTITION BY в синтаксисе оконных функций?
Ответ: Для разбиения строк на группы (разделы), внутри которых вычисляется функция

57. Какое условие является обязательным для того, чтобы представление было автоматически изменяемым?
Ответ: Список FROM содержит ровно одну базовую таблицу

58. Какие ключевые слова являются частью синтаксиса условного оператора IF?
Ответ: THEN, ELSEIF

59. В чем основное отличие функции RANK() от DENSE_RANK()?
Ответ: RANK() пропускает следующий числовой ранг при наличии дубликатов, а DENSE_RANK() – нет

60. Какие операции может автоматически выполнять оператор MERGE в зависимости от условий?
Ответ: INSERT, UPDATE, DELETE

61. Какие из перечисленных операций относятся к специальным реляционным операциям?
Ответ: Проекция отношения, Соединение отношений, Деление отношений

62. Какое указание при объявлении курсора позволяет сделать его чувствительным к изменениям данных?
Ответ: FOR UPDATE

63. Каковы основные свойства общетабличных выражений (СТЕ)?
Ответ: Существует только в рамках текущего запроса, Может использоваться в операторах SELECT, INSERT, UPDATE, DELETE

64. Какова основная характеристика данных при обращении к обычному динамическому представлению (VIEW)?
Ответ: Данные не хранятся физически, а вычисляются на основе базовых таблиц в момент выполнения запроса.

65. Определите назначение предложенных функций для числовых данных
Ответ:
LOG — Логарифм по указанному основанию
SQRT — Квадратный корень числа
EXP — Экспонента (число e в указанной степени)
POW — Число в указанной степени

66. Что является входными данными для компонента оптимизатора запросов?
Ответ: Запрос, схема базы данных и статистика

67. Для чего используется функция CAST в SQL?
Ответ: Явное преобразование значения из одного типа данных в другой

68. Каким образом могут быть переданы данные в базу при вставке или обновлении?
Ответ: В виде потока байтов, С использованием специальных функций или библиотек

69. Какая функция используется для удаления пробелов в начале и в конце строки?
Ответ: TRIM()

70. Какую возможность предоставляют параметры типа INOUT в процедурах?
Ответ: Одновременную передачу данных и возврат измененного значения

71. Какие события могут вызвать автоматическое срабатывание триггера?
Ответ: INSERT, UPDATE

72. Сопоставьте методы обеспечения безопасности с их основными задачами
Ответ:
Аудит кода — Поиск потенциальных угроз
Обновление ПО — Устранение программных уязвимостей
Параметризованные запросы — Защита от SQL-инъекций

73. При каком условии выполняется блок кода внутри цикла WHILE?
Ответ: Пока проверяемое условие остается истинным

74. Какие ключевые слова определяют начальную границу окна?
Ответ: UNBOUNDED PRECEDING, CURRENT ROW

75. Какое расширение необходимо подключить в PostgreSQL для использования функции CROSSTAB()?
Ответ: tablefunc

76. Какие функции предназначены для ранжирования строк в окне?
Ответ: RANK, ROW_NUMBER

77. Какой из перечисленных операторов соединения использует хэширование результатов по ключу?
Ответ: Hash Join (Хэш-совпадение)

78. Какие значения может возвращать триггерная функция, вызываемая для уровня строки?
Ответ: NEW, OLD, NULL

79. Сопоставьте аббревиатуру группы SQL операторов и её назначение
Ответ:
DCL — Операторы определения доступа к данным
DML — Операторы манипуляции данными
DDL — Операторы определения данных
TCL — Операторы управления транзакциями

80. Какова основная задача блока EXCEPTION в программном коде?
Ответ: Перехват и обработка возникающих исключений и ошибок

81. Как ведет себя функция ROW_NUMBER() при наличии повторяющихся значений в столбце сортировки?
Ответ: Вычисляет последовательный ранг (порядковый номер) независимо от повторений

82. Какие типы блокировок используются для управления доступом к данным?
Ответ: Монопольные блокировки (X-блокировки), обеспечивающие блокировку записи; Разделяемые блокировки (S-блокировки), обеспечивающие блокировку чтения

83. Какие функции присваивают одинаковый номер строкам с равными значениями?
Ответ: DENSE_RANK, RANK

84. Какие дополнительные формы соединения позволяют сократить запись условия при совпадении имен столбцов?
Ответ: USING, NATURAL

85. Какое основное назначение команды TRUNCATE в языке SQL?
Ответ: Удаление всех записей из таблицы с сохранением её структуры

86. Выберите верные рекомендации по созданию и использованию индексов:
Ответ:
В индексе по составному ключу поля следует располагать в порядке уменьшения селективности
Не рекомендуется создавать индексы для маленьких таблиц
Не рекомендуется создавать индексы для интенсивно обновляемых таблиц

87. Какой механизм является основным для защиты от SQL-инъекций при работе с двоичными данными?
Ответ: Параметризованные запросы

88. Какие операторы входят в состав языка манипуляции данными (DML) помимо SELECT?
Ответ: INSERT, UPDATE, DELETE

89. Какие утверждения верно описывают назначение и структуру индекса?
Ответ: Индекс ускоряет выполнение операций поиска данных, Индекс является объектом БД и структурой на диске

90. Сопоставьте название функции ранжирования с алгоритмом присвоения ранга при наличии дубликатов.
Ответ:
DENSE_RANK — Одинаковый ранг без пропуска следующего числового ранга
RANK — Одинаковый ранг с пропуском следующего числового ранга
ROW_NUMBER — Порядковый номер строк независимо от наличия повторений

91. Какая команда используется для актуализации данных в материализованном представлении?
Ответ: REFRESH MATERIALIZED VIEW

92. Какое свойство транзакций гарантирует, что результаты работы должны сохраниться даже при последующем сбое системы?
Ответ: Свойство долговечности (Durability)

93. Сопоставьте операторы СУБД со способами получения данных из таблицы.
Ответ:
Sequential Scan (Seq Scan) — Последовательное чтение всех строк таблицы без индексов
Index Only Scan — Извлечение всех необходимых данных напрямую из индекса
Index Scan — Получение данных через индекс с доступом к таблице

94. Какие варианты направления извлечения строк доступны в команде FETCH?
Ответ: NEXT, PRIOR

95. Какие опции доступны в выражениях ON DELETE и ON UPDATE для управления связанными данными?
Ответ:
RESTRICT для предотвращения удаления/изменения при наличии связей
SET NULL для установки неопределенного значения
CASCADE для автоматического изменения зависимых строк

96. Какие утверждения верны для материализованных представлений?
Ответ: Физически хранят результаты запроса, Периодически обновляются, Могут улучшить производительность сложных запросов

97. Какое назначение имеют физические операторы в плане выполнения запроса?
Ответ: Они реализуют конкретные действия по обработке данных

98. Какие утверждения относительно предиката EXISTS являются верными?
Ответ: Принимает значение TRUE, если подзапрос содержит любое количество строк; Принимает значение FALSE, если подзапрос не возвращает строк

99. Какое значение вычисляет аналитическая функция PERCENT_RANK()?
Ответ: Относительный ранг строки в окне

100. Соотнесите ключевое слово SQL с правилом обработки дубликатов и наборов строк
Ответ:
EXCEPT — Получение строк первого набора, отсутствующих во втором
INTERSECT — Поиск общих строк в двух наборах данных
UNION ALL — Объединение нескольких запросов с сохранением дубликатов
UNION — Объединение нескольких запросов без сохранения дубликатов

101. Из каких функциональных частей состоит реляционная модель данных согласно классической теории?
Ответ: Структурная, манипуляционная и целостная

102. Какое из утверждений об общетабличном выражении (CTE) верно?
Ответ: Существует только в рамках текущего запроса и не сохраняется в базе данных

103. Какие предложения могут использоваться в синтаксисе OVER для настройки окна?
Ответ: PARTITION BY, ORDER BY, ROWS или RANGE

104. Для каких объектов базы данных могут быть определены триггеры с пометкой INSTEAD OF?
Ответ: Только для представлений

105. Какие операции над данными фиксирует системная переменная TG_OP?
Ответ: INSERT, DELETE

106. Какие функции используются для округления числовых данных к целому числу или с заданной точностью?
Ответ: ROUND, CEIL, FLOOR

107. Что из перечисленного помогает ускорить доступ к BLOB-данным и оптимизировать ресурсы?
Ответ: Использование соответствующих индексов

108. В чем заключается ключевая особенность материализованного представления?
Ответ: Оно физически хранит на диске результаты выполненного запроса

109. Какие группы операторов выделяются в структуре языка SQL для управления данными и их определения?
Ответ: DDL – операторы определения данных, DML – операторы манипуляции данными

110. Какие ключевые слова могут использоваться с табличными подзапросами в предложении WHERE?
Ответ: IN, EXISTS, SOME (ANY)

111. Где физически хранятся данные при использовании обычного представления (VIEW)?
Ответ: Данные формируются динамически из основных таблиц

112. Какие типы фреймов доступны в SQL для кадрирования окна?
Ответ: ROWS, RANGE, GROUPS

113. В чем заключается требование целостности по ссылкам (referential integrity)?
Ответ: Для каждого значения внешнего ключа должен существовать кортеж в главном отношении с таким же значением первичного ключа

114. Из каких функциональных частей состоит реляционная модель данных согласно классификации К. Дж. Дейта?
Ответ: структурная часть, манипуляционная часть, целостная часть

115. Что произойдет со столбцами, которые не указаны в команде INSERT и не имеют явно заданных значений?
Ответ: Они получат значения по умолчанию

116. Сопоставьте агрегатную функцию и выполняемое ею действие
Ответ:
MIN — Возвращает минимальное значение в указанном столбце
SUM — Возвращает сумму значений в указанном столбце
MAX — Возвращает максимальное значение в указанном столбце
AVG — Возвращает среднее значение в указанном столбце

117. Установите соответствие между специальными переменными триггеров и их назначением.
Ответ:
NEW — содержит новую запись для операций вставки и обновления
TG_NAME — имя триггера
OLD — содержит старую запись до выполнения обновления или удаления
TG_OP — операция, для которой запускается триггерная функция

118. Какие элементы реляционной модели соответствуют стандартному представлению данных в виде таблицы?
Ответ: кортеж – строка таблицы, отношение – таблица

119. Какие виды выражений CASE существуют для реализации условной логики в запросах?
Ответ: Поисковое выражение CASE, Простое выражение CASE

120. Какая проблема возникает при конфликте доступа типа W-R (Запись - Чтение)?
Ответ: Чтение «грязных» данных, которые еще не были зафиксированы

121. Каков формальный порядок логической обработки ключевых слов в запросе SELECT?
Ответ: FROM, WHERE, GROUP BY, HAVING, SELECT, ORDER BY

122. Какие операции входят в состав теоретико-множественных операций согласно реляционной алгебре?
Ответ: Пересечение отношений, Взятие декартова произведения отношений, Взятие разности отношений

123. В каком формате могут быть выведены данные при их извлечении из базы?
Ответ: В виде потока байтов, В виде файла

124. Сопоставьте функции смещения с их описанием
Ответ:
FIRST_VALUE — Первое значение в окне
LAST_VALUE — Последнее значение в окне
LEAD — Данные следующих строк
LAG — Данные предыдущих строк

125. Установите соответствие между типом кадрирования и способом определения фрейма в окне.
Ответ:
ROWS — Смещение строк относительно текущей строки
GROUPS — Учёт всех строк, входящих в одну группу по значению сортировки
RANGE — Логический диапазон значений столбца сортировки

126. Какой объект базы данных предназначен для генерации уникальных числовых значений?
Ответ: Последовательности

127. Что возвращает скалярный подзапрос?
Ответ: Единственное значение

128. Соотнесите оператор подзапроса с логикой формирования результата
Ответ:
ALL — Условие выполняется для всех значений
EXISTS — Проверка наличия строк в подзапросе
IN — Сравнение со множеством значений
ANY — Условие выполняется хотя бы для одного

129. Какие виды конфликтов доступа к данным могут возникать в многопользовательской среде?
Ответ: W-W (Запись - Запись), приводящий к потере обновления; W-R (Запись - Чтение), приводящий к чтению "грязных" данных

130. Какое различие между функцией и хранимой процедурой в PL/pgSQL является верным?
Ответ: Функция обязана возвращать значение, а процедура вызывается командой CALL

131. К какой группе операторов SQL относится команда SELECT?
Ответ: DML (Data Manipulation Language)

132. Какие специальные переменные используются для доступа к состоянию строк в триггерных функциях?
Ответ: OLD, NEW

133. Сколько аргументов может принимать оконная функция смещения LAG()?
Ответ: До трех: столбец, количество строк смещения и значение для замены NULL

134. Что определяет результат выполнения CROSS JOIN?
Ответ:
Формирование декартова произведения двух таблиц
Каждая строка из первой таблицы соединяется с каждой строкой из второй таблицы
Для выполнения не требуется секция ON с условием

135. Что представляет собой индекс в контексте базы данных?
Ответ: Объект БД и структура на диске для ускорения поиска данных

136. Что возвращает операция EXCEPT (разность) при выполнении запроса в SQL?
Ответ: Строки из первого запроса, которые полностью отсутствуют в результате второго

137. В каком случае условие сравнения с ключевым словом ALL считается выполненным?
Ответ: Когда оно выполняется для всех значений в результирующем столбце подзапроса

138. Какие утверждения верны для триггеров типа INSTEAD OF?
Ответ: Срабатывают вместо основной операции, Определяются только для представлений

139. Сопоставьте момент срабатывания триггера с описанием выполняемого действия.
Ответ:
INSTEAD OF — срабатывает вместо операции
BEFORE — срабатывает до операции со строкой
AFTER — срабатывает после завершения операции

140. Как правильно увеличить значение цены в столбце price на 10% с помощью оператора UPDATE?
Ответ: UPDATE products SET price = price * 1.10

141. Что характерно для операции INNER JOIN?
Ответ: Возвращает только те строки, которые имеют совпадение в обеих таблицах; Требует задания условия совпадения в разделе ON

142. Что произойдет с обрабатываемой строкой, если триггерная функция BEFORE ROW вернет значение NULL?
Ответ: Операция над этой строкой будет пропущена

143. Что входит во входные данные оптимизатора запросов?
Ответ: Схема базы данных, включая определения таблиц и индексов; Статистика базы данных

144. Какое действие выполняет опция CASCADE при удалении строки из главной таблицы?
Ответ: Автоматически удаляет связанные строки в зависимой таблице

145. Что представляет собой триггер в контексте баз данных?
Ответ: Механизм автоматического выполнения кода при наступлении событий вставки, обновления или удаления данных

146. Каким оператором завершается условная конструкция IF в языке PL/pgSQL?
Ответ: END IF

147. С какими объектами в базе данных могут быть непосредственно связаны триггеры?
Ответ: Таблицы, Представления

148. В какой момент выполнения SQL-запроса вычисляются оконные функции?
Ответ: После фильтрации WHERE и группировки GROUP BY, но перед финальной сортировкой ORDER BY

149. Какие команды языка определения данных (DDL) используются для изменения структуры или удаления объектов?
Ответ: DROP для удаления объектов, ALTER для изменения структуры

150. Каковы особенности выполнения операции FULL JOIN?
Ответ:
Возвращает все строки, когда есть совпадение хотя бы в одной из таблиц
Позволяет свести воедино два списка без потери данных
Заполняет отсутствующие значения меткой NULL

151. Какие факторы оказывают влияние на построение плана выполнения?
Ответ: Наличие полезных индексов для эффективного доступа к данным; Использованные в запросе предикаты поиска и условия соединения

152. В чем заключается главная особенность операции LEFT ANTI-JOIN?
Ответ: Возвращает только строки из левой таблицы, для которых нет соответствующих строк в правой

153. Установите соответствие между типом соединения таблиц и логикой формирования результирующего набора данных
Ответ:
FULL JOIN — Все строки при наличии совпадения в одной из таблиц
RIGHT JOIN — Все правые строки и совпадающие из левой таблицы
INNER JOIN — Только строки с совпадением в обеих таблицах
LEFT JOIN — Все левые строки и совпадающие из правой таблицы

154. Какие объекты используются для обеспечения целостности и логической группировки в базе данных?
Ответ: Схемы, Ограничения

155. В чем заключается основное отличие операции UNION ALL от UNION в SQL?
Ответ: UNION ALL сохраняет дубликаты строк в результирующем наборе

156. Расставьте элементы структуры блока функции ppgsql в правильном порядке
Ответ:
1. DELCARE
2. BEGIN
3. операторы
4. END

157. Сопоставьте название функции ранжирования с алгоритмом присвоения ранга при наличии дубликатов
Ответ:
RNAK (RANK) — Одинаковый ранг с пропуском следующего числового ранга
ROW_NUMBER — Порядковый номер строк независимо от наличия повторений
DENSE_RANK — Одинаковый ранг без пропуска следующего числового ранга

158. Расположите ключевые слова в порядке их синтаксического следования в запросе
Ответ:

SELECT

FROM

WHERE

GROUP BY

HAVING

ORDER BY


158. Какие операторы относятся к условным конструкциям управления потоком?
Ответ: CASE, IF, ELSEIF

159. Выберите корректные описания свойств ACID для транзакций:
Ответ:
Изоляция гарантирует, что транзакции разных пользователей не мешают друг другу
Атомарность означает, что транзакция выполняется как неделимая операция
Долговечность обеспечивает сохранение результатов работы даже при сбое системы

160. В чем заключается основное различие между использованием предложений WHERE и HAVING?
Ответ: WHERE фильтрует строки до группировки, а HAVING фильтрует агрегированные результаты

161. Какие возможности предоставляет оператор DELETE при работе с BLOB-данными?

Ответ:

Удаление отдельных записей по идентификатору

Применение операторов сравнения и логики

Удаление всех данных, подходящих под условие
